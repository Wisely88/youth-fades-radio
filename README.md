# 青春流逝电台

抖音热门歌曲 · 随机电台

## 特点
- 极简：只有播放/暂停和音量
- 无上一首、下一首、搜索、收藏、歌单
- 播放结束自动随机下一首
- 最近 5 首不重复
- 每首歌支持独立图片与磁带配色

## 替换真实歌曲
把音频放进 `audio/`，图片放进 `images/`，然后编辑 `tracks.json`。

## GitHub Pages
仓库包含 Pages 自动部署工作流。启用 GitHub Pages 的 GitHub Actions 发布源后，推送到 `main` 会自动部署静态网页。
